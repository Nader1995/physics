color = ["red", "blue"]
m = int(2000)               # Number of Vagrants:) ( Random Walkers)
t_max = int(100)            # Maximum time of execution
h = float(1 / 8)           # Length of each time step
counter = int(t_max / h)    # Total step number; to see a more coherent output....
# ... I use "counter" instead of the default N = 100
